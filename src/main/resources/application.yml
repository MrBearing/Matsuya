spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:./DB/tmp/matsuya
    username: sa
    password:
  jpa:
    hibernate.ddl-auto: validate
  thymeleaf.cache: false
security.basic.enabled: false

logging:
    file: logs/myapp.log
    level:
      org.springframework.web: INFO